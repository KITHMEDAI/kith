---
topic: "one AI call asked to both compress a raw session transcript and structure it into a SOAP note tends to do both jobs badly - Kith runs a two-layer pipeline instead, one pass to compress and a separate pass to structure"
sourcePost: null
platforms: ["twitter"]
status: "pending"
---

## Twitter/X

Ask one AI call to compress a raw transcript and structure it into a SOAP note at the same time, and it tends to do both jobs half-well. Kith splits it: one pass compresses, a separate pass structures. Different jobs, different passes.
